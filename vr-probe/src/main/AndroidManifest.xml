<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          android:installLocation="auto">

    <!-- Quest is arm64 GLES3 with 6DOF head tracking -->
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />
    <uses-feature android:name="android.hardware.vr.headtracking"
                  android:version="1" android:required="true" />

    <application android:label="ETR VR Probe"
                 android:allowBackup="false"
                 android:hasCode="false">

        <!-- Meta Quest: run on every headset, keep running under system overlays -->
        <meta-data android:name="com.oculus.supportedDevices" android:value="all" />
        <meta-data android:name="com.oculus.vr.focusaware" android:value="true" />

        <activity android:name="android.app.NativeActivity"
                  android:label="ETR VR Probe"
                  android:launchMode="singleTask"
                  android:excludeFromRecents="false"
                  android:configChanges="screenSize|screenLayout|orientation|keyboardHidden|keyboard|navigation|uiMode|density"
                  android:screenOrientation="landscape"
                  android:exported="true">

            <meta-data android:name="android.app.lib_name" android:value="vrprobe" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- Launch as a native VR (OpenXR) app on Meta Quest -->
                <category android:name="com.oculus.intent.category.VR" />
                <category android:name="org.khronos.openxr.intent.category.IMMERSIVE_HMD" />
            </intent-filter>
        </activity>
    </application>
</manifest>
